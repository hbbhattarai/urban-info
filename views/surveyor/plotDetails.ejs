<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Plot Details</title>

  <!-- Raleway Font -->
  <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Raleway', sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f9f9f9;
    }

    h1 {
      text-align: center;
      font-weight: 600;
      margin-bottom: 30px;
    }

    form {
      max-width: 600px;
      margin: auto;
      background: #fff;
      padding: 25px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    label {
      display: block;
      margin-top: 15px;
      font-weight: 600;
    }

    input[type="file"],
    input[type="checkbox"] {
      margin-top: 5px;
    }

    input[type="checkbox"] {
      margin-right: 8px;
      transform: scale(1.2);
    }

    button {
      margin-top: 25px;
      width: 100%;
      padding: 12px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #45a049;
    }

    .photo-preview {
      margin-top: 15px;
    }

    .photo-preview img {
      height: 100px;
      margin-right: 10px;
      border-radius: 4px;
      border: 1px solid #ddd;
    }

    @media (max-width: 600px) {
      form {
        padding: 20px;
      }
    }
  </style>
</head>

<body>
  <h1>Plot Details for Parcel ID: <%= parcelId %></h1>

  <form action="/editor/surveys/<%= surveyId %>/survey/parcel/<%= parcelId %>/plot" method="POST" enctype="multipart/form-data">
    <input type="hidden" name="parcelId" value="<%= parcelId %>">

    <label>
      <input type="checkbox" id="isConstructed" name="isConstructed" <%= plot?.isConstructed ? 'checked' : '' %> >
      Is Constructed
    </label>

    <label for="facadePhotos">Facade Photos:</label>
    <input type="file" id="facadePhotos" name="facadePhotos" accept="image/*" multiple>

    <% if (plot?.photos?.length) { %>
      <div class="photo-preview">
        <% plot.photos.forEach(photo => { %>
          <img src="<%= photo %>" alt="Plot Photo">
        <% }) %>
      </div>
    <% } %>

    <label>
      <input type="checkbox" id="status" name="status" <%= plot?.status ? 'checked' : '' %> >
      Is Survey Completed
    </label>

    <button type="submit">Submit</button>
  </form>
</body>

</html>
